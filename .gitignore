# Reference checkout — not part of this repo
ccpocket/
codex-terminal/
.worktrees/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
# The workspaces are installed with npm from the root package-lock.json;
# a stray pnpm lockfile in a workspace would fight it.
pnpm-lock.yaml

# Build output
dist/
build/
*.tsbuildinfo

# Bridge runtime data (prompts, transcripts, images)
.config/mac-remote-terminal/
# Session registry, jobs, and profiles. Local state, and sessions.json carries
# project paths and agent-derived session names.
.bridge/

# Env / secrets
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
